name: Keep Render Server Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # runs every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Server
        run: curl https://intern-landing.onrender.com/api/health